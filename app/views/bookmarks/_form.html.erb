<%= simple_form_for [list, bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie, collection: Movie.all.order(:title), input_html: { data: { controller: "tom-select" } } %>
  <%= f.submit "Create movie" %>
<% end %>
